import React from 'react'
import styled from 'styled-components'

const HeaderBlock = styled.header`
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    background: white;
`

export const Header = () => {
    return(
        <HeaderBlock>

        </HeaderBlock>
    )
}